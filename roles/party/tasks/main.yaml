---
- name: execute
  poll: 0
  raw: blinkstick {{ item }}
  tags: ['party']
  loop:
    - random
    - random
    - random

- name: execute
  poll: 0
  raw: blinkstick --morph {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick --blink {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink


- name: execute
  poll: 0
  raw: blinkstick {{ item }}
  tags: ['party']
  loop:
    - random
    - random
    - random

- name: execute
  poll: 0
  raw: blinkstick --morph {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick --blink {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick {{ item }}
  tags: ['party']
  loop:
    - random
    - random
    - random

- name: execute
  poll: 0
  raw: blinkstick --morph {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick --blink {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick {{ item }}
  tags: ['party']
  loop:
    - random
    - random
    - random

- name: execute
  poll: 0
  raw: blinkstick --morph {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: execute
  poll: 0
  raw: blinkstick --blink {{ item }}
  tags: ['party']
  loop:
    - cyan
    - yellow
    - pink

- name: reset
  include_role:
    name: base_config