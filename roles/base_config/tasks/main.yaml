---
- name: execute
  vars:
    color: "{{ base_config.color }}"
    brightness: "{{ base_config.brightness }}"
  shell: blinkstick --set-color {{ color }} --brightness {{ brightness }}
  register: python
  become: true

- name: output
  debug:
    var: python